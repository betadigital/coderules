_schema-version: 3.3.0
ID: SA-Power-CRR
version: 1.0.0
description: "SAP CAP App with Fiori UIs for SA Power code review"
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production

modules:
  - name: SA-Power-CRR-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    requires:
      - name: SA-Power-CRR-auth
      - name: SA-Power-CRR-db
      - name: SA-Power-CRR-destination

  - name: SA-Power-CRR-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: SA-Power-CRR-db

  - name: SA-Power-CRR
    type: approuter.nodejs
    path: app/router
    parameters:
      keep-existing-routes: true
      disk-quota: 256M
      memory: 256M
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api
          url: ~{srv-url}
          forwardAuthToken: true
      - name: SA-Power-CRR-auth
      - name: SA-Power-CRR-destination
      - name: SA-Power-CRR-html5-runtime
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}

  - name: SA-Power-CRR-app-deployer
    type: com.sap.application.content
    path: .
    parameters:
      app-config:
        overwrite: true
    requires:
      - name: SA-Power-CRR-html5-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - name: codereviewrulesbaseruleapp
          artifacts:
            - baseruleapp.zip
          target-path: resources/
        - name: codereviewrulesuserrulesapp
          artifacts:
            - userrulesapp.zip
          target-path: resources/
        - name: codereviewruleslogsapp
          artifacts:
            - logsapp.zip
          target-path: resources/
        - name: codereviewrulesobjecttypes
          artifacts:
            - objecttypes.zip
          target-path: resources/
        - name: codereviewrulestransportoutcomesapp
          artifacts:
            - transportoutcomesapp.zip
          target-path: resources/

  - name: codereviewrulesbaseruleapp
    type: html5
    path: app/baseruleapp
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms: []

  - name: codereviewrulesuserrulesapp
    type: html5
    path: app/userrulesapp
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms: []

  - name: codereviewruleslogsapp
    type: html5
    path: app/logsapp
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms: []
  - name: codereviewrulesobjecttypes
    type: html5
    path: app/objecttypes
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms: []
  - name: codereviewrulestransportoutcomesapp
    type: html5
    path: app/transportoutcomesapp
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms: []

resources:
  # XSUAA Service pointing to your existing instance
  - name: SA-Power-CRR-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        tenant-mode: dedicated
        xsappname: SA-Power-CRR
        oauth2-configuration:
          redirect-uris:
            - https://*.cfapps.*.hana.ondemand.com/login/callback # Generic callback
            - https://*.${default-domain}/login/callback
            - https://*~{app-api/app-uri}/**
        role-collections:
          - name: "RuleAdmin (SA-Power-CRR ${org}-${space})"
            description: "generated"
            role-template-references:
              - "$XSAPPNAME.RuleAdmin"
          - name: "RuleReader (SA-Power-CRR ${org}-${space})"
            description: "generated"
            role-template-references:
              - "$XSAPPNAME.RuleReader"
          - name: "RuleCreator (SA-Power-CRR ${org}-${space})"
            description: "generated"
            role-template-references:
              - "$XSAPPNAME.RuleCreator"
          - name: "RuleM2M (SA-Power-CRR ${org}-${space})"
            description: "generated"
            role-template-references:
              - "$XSAPPNAME.RuleM2m"
    requires:
      - name: app-api

  # HDI Container (HANA)
  - name: SA-Power-CRR-db
    type: org.cloudfoundry.managed-service
    parameters:
      service: hana
      service-plan: hdi-shared

  # Destination Service
  - name: SA-Power-CRR-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite

  # HTML5 Repo Host
  - name: SA-Power-CRR-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host

  # HTML5 Repo Runtime
  - name: SA-Power-CRR-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
